---
id: cookie-secure
title: Secure Cookie
description: |
  If true, instructs browsers to only send user session cookies over HTTPS.
keywords:
  - reference
  - HTTPS only
pagination_prev: null
pagination_next: null
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Secure Cookie

## Summary

If true, **Cookie Secure** instructs browsers to only send user session cookies over HTTPS.

:::warning

Setting this to `false` may result in session cookies being sent in clear text.

:::

## How to configure

| **Type** | **Default** |
| :--- | :--- |
| `boolean` | `true` |

<Tabs>
<TabItem value="Core" label="Core">

| **Config file keys** | **Environment variables** |
| :--- | :--- |
| `cookie_secure` | `COOKIE_SECURE` |

</TabItem>
<TabItem value="Enterprise" label="Enterprise">

In the Enterprise Console, configure `cookie_secure` with the **HTTPS only** toggle button. The button has three states:
- **Unset** ("-") uses the value in your `config.yaml` file
- **Checkmark** sets `cookie_secure` to `true`
- **Empty** sets `cookie_secure` to `false`

<iframe width="100%" height="315" src="https://www.youtube.com/embed/6MatX3Fpgx8" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowfullscreen></iframe>

</TabItem>
<TabItem value="Kubernetes" label="Kubernetes">

**Name:** `cookie.secure`

See Kubernetes [Cookie reference](/docs/deploying/k8s/reference#cookie) for more information.

</TabItem>
</Tabs>

### Examples

```yaml
cookie_secure: false

COOKIE_SECURE=false

cookie.secure: false
```
