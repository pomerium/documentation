Non-sealable: requires a live Pomerium Zero account, API token, and outbound calls to the managed Zero control plane to create routes, policies, and cluster settings. The install script is interactive and provisions infrastructure against a real deployment host, so it cannot run inside the Docker fixture harness.
