Kubernetes guide with no sealed app UI to capture: the Argo Workflows dashboard renders only against a live cluster and identity provider, so there is no deterministic, reproducible UI screenshot to commit.
