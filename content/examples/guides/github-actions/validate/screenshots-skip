No application UI to capture: the integration is a workflow file plus a config file, and verification is an HTTP status code and an authorize-log line rather than a screen. The request flow is shown with the mermaid diagram in the guide.
