# Sealed validation for the OpenClaw gateway guide. Reuses the shared harness
# (Keycloak + certs + headless runner) and adds the OpenClaw gateway + a Pomerium
# wired to the in-network IdP. This is an auth + upstream smoke test: it proves the
# route requires SSO and that an allowed user reaches the gateway. OpenClaw's SSH
# administration and device pairing are exercised manually, not here.
# Run with: scripts/validate-guide-fixtures.sh openclaw-gateway

include:
  - ../../_harness/compose/compose.harness.yaml

services:
  openclaw-gateway:
    build:
      context: ./openclaw
    networks:
      default:
        aliases:
          - openclaw-gateway

  pomerium:
    image: pomerium/pomerium@sha256:e10d1d267af24f581157f485d9b0bc08469e2428675b696a08e42ceb09b2279c
    command: ['--config', '/pomerium/config.yaml']
    env_file:
      - ../../_harness/pomerium/validation.env
    volumes:
      - certs:/certs:ro
      - ./routes.validate.yaml:/pomerium/config.yaml:ro
    depends_on:
      certs-init:
        condition: service_completed_successfully
      keycloak:
        condition: service_healthy
      openclaw-gateway:
        condition: service_healthy
    networks:
      default:
        aliases:
          - authenticate.localhost.pomerium.io
          - openclaw-gateway.localhost.pomerium.io
    healthcheck:
      test: ['CMD', 'pomerium', 'health']
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 10s
