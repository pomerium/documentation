[WebService]
# Validation-only: matches the sealed route host the harness drives.
Origins = https://cockpit.localhost.pomerium.io wss://cockpit.localhost.pomerium.io
ProtocolHeader = X-Forwarded-Proto
LoginTitle = Cockpit
